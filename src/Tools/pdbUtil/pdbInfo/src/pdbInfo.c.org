#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>                  
#define GLOBAL_DECLARATION
#include "../inc/config.h"

void
main(int argc, char* argv[]) 
{
	long status;
	pdbInfoInfo info;

	init0(&info);
    argCheck(&info, argc, argv);
    init1(&info);

	exit(EXIT_SUCCESS);
}

void
additionalUsage()
{
}
