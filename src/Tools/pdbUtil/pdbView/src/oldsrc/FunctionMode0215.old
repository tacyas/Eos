#ifndef FUNCTION_MODE_HH
#define FUNCTION_MODE_HH


enum DisplayMode{
  SpaceFill,
  BackBone,
};

enum AtomMode{
  All,
  CA,
};

enum ColorMode{
  Monochrome,
  CPK,
  Group,
};

enum PickMode{
  Default,
  ResNumber,
  AtomName,
  ResName,
  ChainID,
  Memo,
  Homology,
};

enum HomologyMode{
  AllData,
  Percentage,
  EachData,
  Color
};

enum PickStatus{
  Search,
  Delete,
  Create,
};

/*
enum CG{
  All,
  CAonly
};
*/
/*
enum TextMode{
  ResidueSequenceNumber,
  AtomName,
  ResidueName,
  ChainIdentifier,
  AllSet,
  Homology
};
*/

/*
enum HomologyMode{
  AllView,
  Percentage,
  EachData,
  Color
};
*/
/*
enum ViewerMode{
  SpaceFill=0,
  BackBone=1
};
*/

#endif /* FUNCTION_MODE */
